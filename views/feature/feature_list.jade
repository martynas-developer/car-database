extends ../layout

block content
    h1= title

    ul
        each feature in feature_list
            li
                a(href=feature.url) #{feature.name}
                p #{feature.description}

        else
            li There are features.